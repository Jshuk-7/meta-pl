if let Some(next) = self.lexer.next() {
                //     println!("{next}");
                //     if next.kind == TokenType::Semicolon {
                //         let _ccurly = self.lexer.next().unwrap();
                //     }
                // }